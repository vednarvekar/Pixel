[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd app && PYTHON_BIN=.venv/bin/python npm run start"
healthcheckPath = "/api/images/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
