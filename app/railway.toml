[build]
builder = "NIXPACKS"
buildCommand = "npm ci && pip install -r requirements.txt"

[deploy]
startCommand = "npm run start"
healthcheckPath = "/api/images/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
